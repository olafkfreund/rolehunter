# MkDocs config for Backstage TechDocs.
# Edit at will — Backstage rebuilds on next view.
# https://backstage.io/docs/features/techdocs/getting-started

site_name: rolehunter
site_description: Self-hosted, single-user AI job-hunt copilot: LLM job matching, per-role CV tailoring, cover letters, application tracking, gap analysis. Next.js + Postgres + Docker.

docs_dir: docs

plugins:
  - techdocs-core
